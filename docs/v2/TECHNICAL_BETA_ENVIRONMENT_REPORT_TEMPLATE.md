# Technical Beta Environment Report Template

- Candidate version or commit:
- Local tag or package id:
- Package or install mode used:
- Host OS:
- Session type: `x11` / `wayland` / `tty` / `headless`
- WM or DE:
- GPU or driver details if relevant:
- Profile or pack used:
- Commands run:
- Diagnostics bundle path:
- Screenshots or extra logs if relevant:

## Notes

- Did this match a validated supported environment, a degraded/export-only environment, or an unsupported one?
- Were any environment warnings reported by `status` or `plan`?
