# x11-picom backend

Phase 1 functional backend.

Responsibilities:

- consume generated `active/picom.conf` and `active/shader.glsl`
- offer runtime check helper
- offer foreground run helper (no daemon behavior)
