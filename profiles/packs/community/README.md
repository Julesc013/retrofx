# RetroFX Community Pack (Offline Placeholder)

Drop community profile TOML files into this directory:

- `profiles/packs/community/*.toml`

They will appear in:

- `./scripts/retrofx list`
- `./scripts/retrofx gallery`

No network fetch is implemented in this build.
Use version control or file copy to share/import profiles offline.
